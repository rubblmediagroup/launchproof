name: ci
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read

jobs:
  verify-runtime:
    name: verify / node-${{ matrix.node }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        node: [24, 26]
    steps:
      - uses: actions/checkout@v4
      - name: Dependency lock required
        run: test -f package-lock.json || { echo 'package-lock.json is required for reproducible public CI'; exit 1; }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --ignore-scripts
      - run: npm run format:check
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build -w @launchproof/web
      - run: npm run build -w @launchproof/cli
      - run: npm pack -w @launchproof/cli --dry-run
      - name: Runtime dependency audit
        run: npm audit --omit=dev --audit-level=high
      - name: Controlled release scenarios
        run: |
          npm run cli -- analyze scenarios/production-reference --json /tmp/launchproof-reference.json
          npm run cli -- analyze scenarios/missing-tenant-authorization --json /tmp/launchproof-regression.json
          npm run verify:scenarios -- /tmp/launchproof-reference.json /tmp/launchproof-regression.json
      - name: LaunchProof self-analysis
        run: npm run cli -- analyze . --json /tmp/launchproof-self.json

  release-surfaces:
    name: release surfaces / node-24
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: verify-runtime
    steps:
      - uses: actions/checkout@v4
      - name: Dependency lock required
        run: test -f package-lock.json || { echo 'package-lock.json is required for reproducible public CI'; exit 1; }
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --ignore-scripts
      - name: Install browser for E2E
        run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
      - name: Docker Compose validation
        run: docker compose config
      - name: Docker build
        run: docker build -t launchproof-ci .

  windows-desktop:
    name: windows desktop / node-24
    runs-on: windows-latest
    timeout-minutes: 35
    needs: verify-runtime
    steps:
      - uses: actions/checkout@v4
      - name: Dependency lock required
        shell: pwsh
        run: if (-not (Test-Path package-lock.json)) { throw 'package-lock.json is required for reproducible public CI' }
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - uses: dtolnay/rust-toolchain@stable
      - run: npm ci --ignore-scripts
      - name: Tauri desktop build
        run: npm run build -w @launchproof/desktop
