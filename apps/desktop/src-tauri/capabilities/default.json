{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal LaunchProof desktop webview capability set.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
